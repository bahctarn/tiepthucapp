<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="Tiếp Thực"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.TiepThuc"
        android:usesCleartextTraffic="false">

        <!--
            Ứng dụng không khai báo bất kỳ permission INTERNET / ACCESS_NETWORK_STATE nào.
            Điều này đảm bảo ứng dụng không thể gọi mạng ngay cả khi có lỗi lập trình,
            vì Android sẽ chặn mọi request mạng nếu thiếu permission "android.permission.INTERNET".
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TiepThuc"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
